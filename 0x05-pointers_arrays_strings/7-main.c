#include "main.h"

/**
 * main - check the code
 *
 * Description: Calls puts_half  function that prints half of a string,
 * followed by a new line.
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "0123456789";
	puts_half(str);
	return (0);
}
