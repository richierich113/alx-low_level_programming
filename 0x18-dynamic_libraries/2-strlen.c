#include "main.h"
#include <string.h>

/**
 *  _strlen -  returns the length of a string.
 * @s: The parameter string to get the length of.
 *
 *  Return: the length of s.
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
