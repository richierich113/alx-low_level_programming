#include <stdio.h>
/**
 * main - a program that prints a line with puts funstion followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzz
le");
return (0);
}

