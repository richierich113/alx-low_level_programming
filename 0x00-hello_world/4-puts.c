#include <stdio.h>
/**
 * main - a program that prints a line with puts function followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
