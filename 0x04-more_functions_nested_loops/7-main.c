#include "main.h"

/**
 * main - draws a diagonal line on the terminal.
 *
 * Description: draws a diagonal line on the terminal.
 * Return: 0
 */
int main(void)
{
	print_diagonal(0);
	print_diagonal(1);
	print_diagonal(2);
	print_diagonal(3);
	print_diagonal(4);
	print_diagonal(5);
	print_diagonal(6);
	print_diagonal(7);
	print_diagonal(8);
	print_diagonal(9);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}
