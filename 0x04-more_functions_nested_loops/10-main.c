#include "main.h"

/**
 * main -  prints a triangle using #.
 *
 * Description: prints a triangle using #, followed by a new line.
 * Return: 0
 */
int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
