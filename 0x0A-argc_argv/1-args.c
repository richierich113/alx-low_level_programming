#include <stdio.h>
#include "main.h"

/**
 * main - print argument count
 * @argc: arg count
 * @argv: array of pointers to arg values
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
