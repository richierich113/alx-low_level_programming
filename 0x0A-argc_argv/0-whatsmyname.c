#include <stdio.h>
#include "main.h"

/**
 * main - print program name
 * @argc: arg count
 * @argv: array of pointers to arg values
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
